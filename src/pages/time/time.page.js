/* eslint-disable no-unused-vars */

import durationDays from '@/components/cards/duration-days';
import durationHours from '@/components/cards/duration-hours';

import template from './time.page.html';

export default {
  name: 'Examples',
  template,
  components: {
    durationDays,
    durationHours,
  },
};
